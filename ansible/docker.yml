---
## Prepare the Ubuntu servers
- name: Setup Docker and services
  hosts:
    - s7home
  become: true  # All shell commands as 'root'
  tasks:
    - import_role:
        name: docker
      tags: docker
